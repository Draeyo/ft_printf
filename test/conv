%s = string
%S = %s w/ l (ell) modifier (wchar_t -> accents ?)
%p = addr en hexa ??
%d = int
%D = %d w/ l (ell) modifier // long
%i = signed decimal (%d ?)
%o = (int ->) unsigned octal
%O = (long ->) unsigned octal
%u = (int ->) unsigned decimal
%U = (long ->) unsigned decimal
%x = (int ->) unsigned hexa (0x)
%X = %x (0X)
%c = (int ->) unsigned char
%C = %c w/ l (ell) modifier
%% = echappement pour '%'
# = ???
0 = (padding)
- = negative field width flag -> value left ajusted on the field
+ = (placed before a number produced by a signed conversion)
hh = di -> signed char / ouxX -> unsigned char / n -> signed char *
h = di -> short / ouxX -> unsigned short / n -> short *
l = di -> long / ouxX -> unsigned long / n -> long *
ll = di > long long / ouxX -> unsigned long long / n -> long long *
j = di -> intmax_t / ouxX -> uintmax_t / n -> intmax_t *
z = di -> signed type eq/ int size to size_t / ouxX -> size_t / n -> pointer to a signed types eq/ in size to size_t
